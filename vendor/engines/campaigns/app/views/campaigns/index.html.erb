<% content_for :body_content_left do %>
  <ul id="campaigns">
    <%= render :partial => "campaign", :collection => @campaigns %>
  </ul>
  
  <%= will_paginate @campaigns, :class => "apple_pagination" %>
<% end %>

<%= render :partial => "/shared/content_page" %>
